import { Container } from "react-bootstrap";
import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <Container>Zorg voor elkaar</Container>
    </footer>
  );
};

export default Footer;
